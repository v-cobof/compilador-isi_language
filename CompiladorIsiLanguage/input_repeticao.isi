programa
    declare numero a, b.

    a := 0.
    leia(b).

    enquanto (a < b)
    {
        escreva (a).
        a := a+1.
    }

    a := 0.

    faca {
        escreva (a).
        a := a+1.
    } enquanto (a < d).

fimprog.