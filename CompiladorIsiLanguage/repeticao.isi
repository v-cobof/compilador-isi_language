programa



    declare numero a, b, c, d.

    leia(a).
    leia(b).

    enquanto (a > b)
    {
        escreva (a).
        a := a+1.
    }

    a := 0.
    b := 5.

    c := 4.5 + 3 / 7.897.

    leia(c).

    escreva("pula linha").
    escrevaNaLinha("não pula linha").

    escreva(c).

    leia(d).

    faca {
        escreva (a).
        a := a+1.
    } enquanto (a > d).

fimprog.