programa

    declare numero a, b, c, d.

    a := 0.
    b := 5.

    leia(c).

    escreva("pula linha").
    escrevaNaLinha("não pula linha").

    escreva(c).

    enquanto (a > b)
    {
        escreva (a).
        a := a+1.
    }

    faca {
        escreva (a).
        a := a+1.
    } enquanto (a > d).

fimprog.